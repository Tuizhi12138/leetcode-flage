class Solution:
    def isPalindrome(self, x: int) -> bool:

        x = list(str(x))

        stat = True
        for i in range(1,len(x)+1):
            if x[i-1]!=x[-i]:
                stat=False
                break

        return stat
