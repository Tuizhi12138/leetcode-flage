class Solution:
    def addDigits(self, num: int) -> int:
        
        while num>9:
            tmp = 0
            while num:
                a = num%10
                num = num//10
                tmp += a
            num = tmp
            
        return num
