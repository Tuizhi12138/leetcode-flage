class Solution:
    def intersection(self, nums1: List[int], nums2: List[int]) -> List[int]:
        s1 = set()
        for i in nums1:
            if i in nums2:
                s1.add(i)
                
        return s1
